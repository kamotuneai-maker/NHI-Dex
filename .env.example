# NHI-Dex Environment Configuration
# Copy to .env and fill in your values

# Required: Anthropic API key for demo agents
ANTHROPIC_API_KEY=your-api-key-here

# Optional: Postgres (defaults work with docker-compose)
POSTGRES_USER=nhidex
POSTGRES_PASSWORD=nhidex-dev
POSTGRES_DB=nhidex
